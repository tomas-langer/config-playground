# increase priority
config_priority: 101
# change to 5 seconds
config_polling_duration: "PT5S"

server:
    host: "0.0.0.0"
    port: 8026

# Requires defined order
providers:
    - type: "yaml-type"
      config:
          enabled: true
          message: "yaml-hi"
    - class: "yaml-fqcn"
      optional: true

